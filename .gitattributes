* text=auto eol=lf

# Certificates are evidence. Git must not rewrite a byte of them -- and one of
# them uses CRLF, which is itself worth carrying through to the reader.
fixtures/*.cnf -text
fixtures/*.drat -text
